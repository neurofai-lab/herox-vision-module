# Examples

This folder is reserved for reproducible D4 execution assets.

Recommended additions before submission:

- a short rosbag containing synchronized RGB, depth, and camera-info topics;
- a mock publisher for synthetic RGB-D input;
- expected terminal logs;
- screenshots of `/humans/bodies/tracked` and `/vision/bounding_boxes_3d` outputs.
